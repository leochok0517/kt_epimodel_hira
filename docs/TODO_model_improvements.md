# 모델 개선 To-Do (발표 피드백 + 자체 발견)

> calibration 마무리 후 다룰 항목. 둘 다 "R(0)를 더 현실적으로"로 수렴.

---

## TODO-1: 백신을 S 뿐 아니라 R(0)에도 적용 (우선순위: 높음)

### 문제
현재 가정: 백신은 S(미면역)만 맞고 V로 이동. R(이미 면역)은 백신 안 맞음.

현실: 백신 맞는 사람은 자기 면역 상태 모름 → 이미 면역(R)인 사람도 맞음.
- 노인: R(0) 높음(0.65) + 접종률 가장 높음(82%)
- R에 있는 노인 상당수가 백신 맞는데 모델이 무시
- 접종 노력 상당 부분이 이미 면역인 사람에게 "낭비"되는데
  모델은 전부 S→V로 효과 낸다고 과대평가

### 왜 중요
- VE=0.5와 별개 문제. VE는 "맞은 사람 중 효과 비율",
  이건 "누가 맞느냐(S vs R)"의 문제.
- R(0)를 연령별 정교화(노인 0.65)했는데 백신은 S만 → 두 가정 불일치.
- 정책 시뮬레이션이 노인 백신 다루면 효과 과대평가 위험.

### 접근
- 백신을 S와 R 양쪽에 인구 비례 접종 (접종률 82%면 S의 82%, R의 82%)
- R 접종 → 효과 0 (이미 면역)
- S 접종 → VE 확률로 V 이동
- R로 간 접종분은 효과 없이 소진 → 현실적

### 검증
- 기존 S-only 대비 노인 결과 차이 (회귀)
- R(0) 높은 그룹에서 접종 효과 희석 확인

---

## TODO-2: post-pandemic 낮은 R(0) 시즌 검증 (우선순위: 중)

### 배경 (발표 피드백)
코로나 기간(2020-2022) 인플루엔자 거의 소멸 → 자연 감염 면역 거의 안 쌓임
→ 2022-23 시즌 시작 시 인구 면역(R(0))이 평년보다 낮았을 가능성.
현재 모든 시즌 동일 R(0) 계단 고정 → 2022-23엔 안 맞을 수 있음.

### 제안
유행 없던 시즌을 낮은 R(0)로 두고 2022-23 fitting 품질 확인.
2022-23이 우리 데이터의 post-pandemic 첫 시즌 → 이 시즌만 R(0) 낮춰
다른 시즌과 비교하면 깔끔한 검증. 잘 맞으면 R(0) 외부 근거 (문헌 보완).

### ★ 주의 (식별성 함정)
- R(0)도 β·φ·γ와 얽혀 비식별 위험
- R(0) "추정"하려 들면 ridge 재발
- → "추정" 아니라 "2022-23을 낮은 R(0) 시나리오로 고정 후 검증/forward"
- R(0)≈0은 비현실 (백신은 코로나 중에도 접종, 잔여 면역) → "낮음"이지 "0" 아님

---

## 공통 인사이트
두 항목 모두 "R(0)를 고정 계단으로 단순화한 것"의 한계:
- 실제 R(0)는 (a) 시즌마다 다르고(팬데믹), (b) 백신과 상호작용
- 현재 단순화는 1차 근사로 OK, 위 개선은 정교화 단계

## 진행 순서
1. (지금) calibration 마무리 — φ 처리 결정 → 본 추정
2. TODO-1 (백신 S+R) — 단순 수정, R(0) 일관성, 노인 정책 정확도
3. TODO-2 (post-pandemic R0) — 검증/sensitivity
4. 둘 다 발표/논문 limitation·future work에 기재

---

## TODO-3: 관측 노이즈 모델 — Poisson → 과분산 ✅ 완료 (2026-06-10)

**결과**: NB(2) 적용, coverage 12%→**95.2%**, r_hat 2.38→**1.02**, ess 5→581,
R0 평균 보존 (1.96→1.90), CI 현실화 (3–4%). `phi_nb` = 1.44 [1.30, 1.59] 식별.
부가: 2017–18 channel swap (practical 비식별) 까지 해소 (좁은 Poisson likelihood가
원인이었음).

---

### (배경 — 원안 보존)


### 배경 (posterior predictive 발견)
production posterior predictive: peak 시점·형태는 잘 재현하나
95% coverage 12.2% (nominal 95% 대비 낮음).

### 원인
1. posterior parameter 매우 정밀 (R0 CI<0.1%) → 예측 띠 좁음
2. ★ 관측 노이즈를 Poisson으로 단순화 → 청구 데이터 과분산 미반영
   - 실제 청구: 주별 변동, 보고 지연, 요일효과 → 분산 > 평균
   - Poisson(분산=평균)은 이를 과소평가 → 띠 좁아 coverage 낮음
3. peak가 obs보다 약간 높고 이름 (systematic residual)
   - seasonal peak_day 고정(105)과 시즌별 실제 peak 차이 가능

### 접근
- 관측 모델 Poisson → Negative Binomial (과분산 파라미터 추가)
  → 예측 띠가 적절히 넓어져 coverage 개선 예상
- (옵션) peak_day 시즌별 조정 — 단 amplitude는 β와 비식별이니 고정
  (timing만, 곱 비식별 주의)

### 주의
- R0 추정 자체는 견고 (3/4 시즌 CI<0.1%) — 이건 관측모델 개선과 무관
- NB 과분산 파라미터도 식별성 점검 필요 (추가 자유도)

### 검증
- NB 적용 후 coverage 95% 근처 회복되는지
- R0 추정 불변 확인 (관측모델 바꿔도 R0 같아야)

---

## TODO-4: 계절성 forcing 재검토 (우선순위: 높음) — 채널 민감도로 확정

### 배경
school_closure 가 49 개 채널 조합 전부에서 80%+ averted (43/49 >90%, 0/49
<80%). 채널 분배와 무관 → 원인은 **계절성 phase-transition**.
cosine amp=0.7: peak window 6-8 주만 R_eff>1, 작은 개입도 epidemic 차단.

### ★ 먼저 확인: calibration ↔ forward 계절성 정합
- calibration (슬라이드 16) 은 Gaussian 계절성 fit (amp/base/σ/peak_day)
- forward (Step C) 는 cosine amp=0.7 사용
- ★ 둘이 다르면 절벽이 "불일치 버그" 일 수 있음 (구조 문제 아니라)
- 먼저 calibration 이 쓴 계절성 함수와 forward 계절성 일치 확인
  → 불일치면 forward 를 calibration 계절성으로 맞춤 (버그 수정)
  → 일치하는데도 절벽이면 구조 문제 (amp sweep)

### 접근 (정합 확인 후)
- amp sweep [0.3, 0.5, 0.7] → school_closure 효과 변동
- 문헌 인플루엔자 R_eff 시계열과 비교 (amp 0.3-0.5 가능성)
- peak window 폭이 현실적인지

---

## TODO-5: home spillover 모델 재검토 (우선순위: 높음)

### 배경
sick_leave 효과 부호가 work π + home π 에 의존 (채널 민감도 발견).
work 격리 → `home FOI += κ·ρ·(1-p_work)` → home 전파 증가가 work 감소와 경쟁.
work π 작으면 (home 상대 큼) spillover 가 이겨 역효과 (음수).

### 접근
- spillover κ (0-19: 0.42, 20-69: 0.60) 적정값 검토
- 한국 가정 격리 시 실제 가구 전파 증가율 문헌
- κ sweep → sick_leave 부호 안정성
- spillover off vs on 비교 (효과 크기)

---

## TODO-6: work:other 비율 외부 추정 (우선순위: 높음) — 신규

### 배경
sick_leave 결론을 좌우하는 핵심 가정. 현재 NIMS row-sum (0.349) 단일값.
work π=0.11 (0.349 기준) → sick_leave 음수. work π ≥ 0.25 면 양수.
→ **비율 가정이 정책 부호를 바꿈**. 단일값 정당화 약함.

### 접근
- KOSIS 직장 출근 패턴, 한국 인플루엔자 surveillance
- 직장 전파 비중 문헌 (한국 / 유사 국가)
- 확정 어려우면 → 민감도 범위로 보고 ("+6.7% ~ −7.2%, 비율 의존")

---

# 세미나 피드백 (2026-06-18 발표)

좌중 요지: 방향·결론 수용. 단 home/work=0 (비식별) 이 정말 데이터 해상도
한계인지, 아니면 (a) 채널 과분해 (b) 초기조건 오류 (c) NIMS contact 편향
때문인지 추가 검증 요구. **대안 원인 배제가 핵심.**

## SEM-1: ★ NIMS contact matrix 신뢰도 검증 (우선순위: 높음)

### 왜 제기됐나
- NIMS 접촉 수가 전반적으로 낮음 — 패널 설계 편향 의심
- NIMS 20대 contact 최저 — 코로나 영향 + 세대 행동변화 해석되나 의심
- 유럽 COMIX 등 타 데이터와 패턴 차이 지적
- 손 박사 공유 자료: COMIX 방법론이 POLYMOD (우편 회고) 대비 부각

### 우리 결론과의 관계 (★ 중요)
- NIMS 는 (a) unit R0 계산 (b) 채널 prior 근거 A 에 쓰임 → 편향이면 둘 다 흔들림
- work=0 이 "데이터 해상도" 가 아니라 "NIMS work (20대) 과소측정" 일 수도
- = **결론을 바꿀 수 있는** 지적

### 검증
- NIMS vs COMIX / POLYMOD 연령별·채널별 패턴 비교
- NIMS 20대 work contact 가 비정상적으로 낮은가
- 대안 contact (COMIX) 로 unit R0·채널 prior 재계산 시 work 살아나나
- 패널 설계 편향 정량화 (표본 크기, 연령 분포)

## SEM-2: 채널 합산 단일 matrix 피팅 (우선순위: 높음)

### 왜 제기됐나
- "채널 고정하고 단일 컨택 매트릭스로 피팅 후 수렴 먼저 확인"
- 4 채널 분해 대신 1 개로 합치면 깔끔히 수렴하나

### 우리 결론과의 관계
- 단일 matrix 수렴 잘 됨 → "채널 분해가 과파라미터화" 확인
  → home/work 비식별이 데이터 한계임을 반대 방향에서 입증 (**결론 강화**)
- 수렴 안 됨 → 다른 문제 (초기조건 / 데이터)

### 검증
- 4 채널 합산 → 단일 contact matrix → R0 + 계절성 + 방학만 추정
- 수렴 (r_hat, NLL), fit 품질 단일 vs 4 채널 비교
- sanity check 성격 (빠름)

## SEM-3: 초기조건 — R 인구 / 초기 감염자 (우선순위: 중간)

### 왜 제기됐나
- "초기 R 인구 거의 0 가정 시나리오" (특히 post-pandemic)
- "초기 감염자 수 설정값 영향 점검"
- 22-23 시즌이 상대적으로 잘 맞음 → 면역구조 시즌별 차이

### 우리 결론과의 관계
- 기존 [[TODO-1 (vaccine S+R)]], [[TODO-2 (post-pandemic R0)]] 와 연결
- R(0) 가정이 시즌별로 틀리면 home/work=0 의 또 다른 원인 후보
- 코로나 후 면역 리셋 → 22-23 다르게 fit 되는 이유

### 검증
- R(0) ≈ 0 시나리오 (post-pandemic, 면역 리셋 가정) 피팅
- 초기 감염자 수 연령별 독립 설정 영향
- 4 시즌 multi-season fit 에서 연령별 피크 안 맞는 문제와 연결

## SEM-4: 발표 내용 업데이트 (정책 효과)

### 왜
- 세미나 때 "학생 50% 결석 → R0<1" (점추정 98% 절벽) 발표함
- 이후 교정: 점추정 절벽 → posterior 38-87% (절벽 희석)

### 할 것
- 다음 발표 슬라이드: "R0<1 감소" → "posterior 38-87%, robust 양수"
- 점추정 절벽 한계 명시 (이미 v4 정리에 반영 중)

## SEM-5: 연령별 susceptibility prior (이미 처리, 기록)

### 상태
- 좌중: 연령별 susceptibility 추정값 들쭉날쭉 → prior 좁게 설정 정당
- = 우리 φ=1.0 고정과 같은 논리 (이미 함)
- 추가 작업 불필요, 근거 기록만

## 우선순위 요약

1. **SEM-1** NIMS 신뢰도 (결론 바꿀 수 있음 — 최우선)
2. **SEM-2** 단일 matrix (빠른 sanity, 결론 강화)
3. **SEM-3** 초기조건 (TODO-1/2 와 통합)
4. **SEM-4** 발표 업데이트 (v4 에서 처리 중)
5. **SEM-5** 기록만
