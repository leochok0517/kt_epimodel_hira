[project]
name = "kt-epimodel-hira"
version = "0.1.0"
description = "HIRA-based influenza calibration (sister project of kt_epimodel)"
requires-python = ">=3.12"
dependencies = [
    "kt-data",
    "scipy",
    "numpy",
    "matplotlib",
    "polars",
    "pandas",
    "joblib>=1.5.3",
    "mlflow>=3.13.0rc0",
    "jax<0.5",
    "jaxlib<0.5",
    "diffrax>=0.7.2",
    "numpyro>=0.19.0",
    "arviz>=1.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kt_epimodel_hira"]

[tool.uv.sources]
kt-data = { path = "../kt_data", editable = true }

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest",
    "ipykernel",
    "jupyter",
]
